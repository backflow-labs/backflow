services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - backflow-data:/data
    restart: unless-stopped

volumes:
  backflow-data:
